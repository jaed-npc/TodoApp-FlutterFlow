{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "User",
    "type": "object",
    "properties": {
      "birthdate": {
        "type": "string",
        "format": "date-time",
        "description": "Birthdate of the user, stored as a timestamp."
      },
      "created_time": {
        "type": "string",
        "format": "date-time",
        "description": "The time the user account was created, stored as a timestamp."
      },
      "display_name": {
        "type": "string",
        "description": "The display name of the user."
      },
      "email": {
        "type": "string",
        "format": "email",
        "description": "The email address of the user."
      },
      "uid": {
        "type": "string",
        "description": "The unique identifier (UID) for the user."
      }
    },
    "required": ["birthdate", "created_time", "display_name", "email", "uid"],
    "additionalProperties": false
  }
  